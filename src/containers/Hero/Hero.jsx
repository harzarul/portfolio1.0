import {Navbar} from '../../components';
import styles from '../../styles';
import {motion} from 'framer-motion';

const Hero = () => {
  return (
    <div>
      <Navbar/>
      Hero
    </div>
  )
}

export default Hero